---
type: migration-phase
from: v0.34.0
to: v0.35.0
created: 2026-06-20
session: s01-bf2abd
---

# Migration v0.34.0 → v0.35.0

## Doctor Actions

```json
{
  "settings-defaults": {},
  "settings-subkeys": {},
  "scaffold-files": []
}
```

No `doctor --fix` settings actions this release (no settings keys changed). The `_memory.md` template
update is handled by the archive-byte-match auto-update (boot sentinel `template-auto-update-v0.35.0`
+ `migrations/gap-safe-fix.mjs`), not by this JSON.

## What changed (user-data relevant)

**Template changes (`templates/default/`) — the "ground before think" generics + SX-795:**

- **`_memory.md`** (framework-owned, `_`-prefixed) — added the **verified-vs-inherited** amnesia-check
  bullet + the **orient-by-live-artifact** Start Here note. `soma_template_version` 0.32.0 → 0.35.0.
  Archived: `templates/default/v0.32.0/_memory.md`.
- **`soul.md` + `core_rules.md`** (**user-owned**, non-`_`) — added "Ground before I think; think before
  I act" (soul) and renamed/strengthened "Probe Before Reason" → "Ground Before Reason" (core_rules).
  `soma_template_version` 0.29.0 → 0.35.0. Archived: `templates/default/v0.29.0/{soul,core_rules}.md`.

## Actions (migration mechanism)

- **`_memory.md`:** `applyOnceAlways("template-auto-update-v0.35.0", …)` in `soma-boot.ts` — runs once
  on the first v0.35.0 boot. Overwrites a project's `_memory.md` ONLY if it byte-matches a known archive
  (`templates/default/v*/`) = provably pristine-old; a customized `_memory.md` matches no archive → KEPT.
  Mirrors the v0.32.0 sentinel + `migrations/gap-safe-fix.mjs` (the `doctor --fix` path).
- **`soul.md` / `core_rules.md`:** NO auto-update — they are user-owned identity/rules files. Only NEW
  installs receive the updated seed; existing users keep theirs (never clobbered). Intentional: the
  framework never overwrites a user's soul.

## Settings

No settings keys added / removed / renamed this release. (ADR 011's settings-resolution work is NOT in
this cut — it's SX-788, a future release.)

## Code (not user-data, FYI)

- SX-795: `edit-tool-coercion.py` + `apply-patches.sh` wiring — coerces the Opus `oldText2`/`newText2`
  Edit mis-shape in the upstream Pi edit tool. Re-applied on each Pi bump; no user-data impact.
- SX-793: `apply-patches.sh` error-sanitizer import-injection hardened (prepend + fail-loud).

## Verify

- **New install:** `soul.md` / `core_rules.md` / `_memory.md` carry the new content + `soma_template_version: 0.35.0`.
- **Existing pristine `_memory.md`:** auto-updates on first v0.35.0 boot (`debug.boot` logs "updated N pristine").
- **Existing customized `_memory.md`** (meetsoma and peer soma instances): KEPT untouched (matches no archive).
- **Existing `soul.md` / `core_rules.md`:** untouched regardless (user-owned).
