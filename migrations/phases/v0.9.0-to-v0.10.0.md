---
from: "0.9.0"
to: "0.10.0"
migration-required: false
breaking: false
---
# v0.9.0 → v0.10.0

## What Changed
- Pi runtime 0.64.0 → 0.66.1 (bug fixes, no extension API changes)
- Scripts reorganized — 39 dev scripts moved to `_dev/`, 3 promoted to bundled
- CLI syntax: `soma-code.sh find` → `soma code find` (script routing via CLI)
- Soma themes (dark/light) + unified build pipeline
- `soma github` — scan GitHub repos without cloning
- Keepalive auto-pause on 429/rate limit
- Guard: sanitize orphaned tool_results before API call
- Docker sandbox E2E environment

**Actions:**
- No user action required
- Settings additions (inherit, keepalive, heat.autoDetectBump) are auto-added by doctor
- CLI syntax change is backward-compatible (old paths still work)
